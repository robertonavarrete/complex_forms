<%= form_with(model: zombie, local: true) do |form| %>
  <% if zombie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zombie.errors.count, "error") %> prohibited this zombie from being saved:</h2>

      <ul>
      <% zombie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <ul>
  <%= form.fields_for :victims do |victims_form| %>
    <%= render 'victims/victims_fields', victims_form: victims_form %>
  <%end %>
</ul>

  <div class="actions">
    <%= add_victim_button(form) %>
    <%= form.submit %>
  </div>
<% end %>
